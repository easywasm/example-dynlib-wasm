{
  "name": "example-dynlib-wasm",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "author": "konsumer",
  "license": "MIT",
  "scripts": {
    "test": "make test",
    "lint": "npx -y prettier --write ."
  },
  "description": "Experimenting with dynamic linking in WebAssembly",
  "dependencies": {
    "@easywasm/wasi": "^0.0.8"
  }
}
